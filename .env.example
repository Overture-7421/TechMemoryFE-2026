# URL of the deployed backend (no trailing slash), e.g. your Render service.
# Used only in production builds — local dev stays relative via the vite
# proxy in vite.config.js and doesn't need this set.
VITE_API_BASE_URL=https://your-server.onrender.com
